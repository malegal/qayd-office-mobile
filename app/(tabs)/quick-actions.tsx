import { useEffect, useMemo, useState } from "react";
import { ActivityIndicator, KeyboardAvoidingView, Platform, Pressable, ScrollView, Text, TextInput, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { useColors } from "@/hooks/use-colors";
import { useOfflineSync } from "@/hooks/use-offline-sync";
import { getCases, type CaseRow } from "@/lib/office-lists";
import { getCurrentMembership, type OfficeMembership } from "@/lib/office-data";
import { queueExpense, queueFee, queueIncome, queuePayment, queueSession } from "@/lib/office-actions";
type ActionType = "session" | "expense" | "income" | "fee" | "payment";
const actions: Array<{ key: ActionType; label: string; icon: "calendar.badge.clock" | "wallet.pass.fill" | "briefcase.fill"; hint: string }> = [{ key: "session", label: "ترحيل جلسة", icon: "calendar.badge.clock", hint: "إضافة جلسة للقضية" }, { key: "expense", label: "مصروف", icon: "wallet.pass.fill", hint: "مكتب أو قضية أو ملف" }, { key: "income", label: "متحصل", icon: "wallet.pass.fill", hint: "إيراد للمكتب أو ملف" }, { key: "fee", label: "رسم / أتعاب", icon: "briefcase.fill", hint: "إنشاء إجمالي أتعاب" }, { key: "payment", label: "دفعة أتعاب", icon: "wallet.pass.fill", hint: "دفعة مرتبطة بقضية" }];
function Field({ label, value, onChangeText, placeholder, keyboardType = "default" }: { label: string; value: string; onChangeText: (value: string) => void; placeholder: string; keyboardType?: "default" | "numeric" }) { const colors = useColors(); return <View className="mb-4"><Text className="text-sm font-bold text-foreground mb-2">{label}</Text><TextInput value={value} onChangeText={onChangeText} placeholder={placeholder} placeholderTextColor={colors.muted} keyboardType={keyboardType} className="rounded-2xl px-4 py-3 text-foreground border border-border" style={{ backgroundColor: colors.background, textAlign: "right" }} /></View>; }
export default function QuickActionsScreen() {
  const colors = useColors(); const [membership, setMembership] = useState<OfficeMembership | null>(null); const [cases, setCases] = useState<CaseRow[]>([]); const [selected, setSelected] = useState<ActionType>("expense"); const [scope, setScope] = useState<"office" | "case" | "file">("office"); const [caseId, setCaseId] = useState(""); const [fileId, setFileId] = useState(""); const [date, setDate] = useState(new Date().toISOString().slice(0, 10)); const [amount, setAmount] = useState(""); const [category, setCategory] = useState("مصروفات تشغيلية"); const [description, setDescription] = useState(""); const [status, setStatus] = useState(""); const [receipt, setReceipt] = useState(""); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); const [caseSearch, setCaseSearch] = useState(""); const offline = useOfflineSync(membership?.office_id);
  useEffect(() => { getCurrentMembership().then(async (m) => { setMembership(m); if (m) setCases(await getCases(m.office_id)); }).catch(() => setError("تعذر تحميل عضوية المكتب.")); }, []); const selectedAction = useMemo(() => actions.find((item) => item.key === selected)!, [selected]); const caseOptions = cases.filter((item) => `${item.case_code ?? ""} ${item.client_name ?? ""} ${item.opponent_name ?? ""} ${item.case_number ?? ""}`.toLowerCase().includes(caseSearch.toLowerCase()));
  const submit = async () => { if (!membership) return; const needsAmount = ["expense", "income", "fee", "payment"].includes(selected); if (needsAmount && (!amount || Number(amount) <= 0)) return setError("أدخل مبلغًا صحيحًا."); if (["session", "fee", "payment"].includes(selected) && !caseId) return setError("اختر القضية أولًا."); if (scope === "file" && !fileId.trim()) return setError("أدخل رمز الملف."); setSaving(true); setError(""); setStatus(""); setReceipt(""); try { let item: { operationId?: string } | undefined; if (selected === "session") item = await queueSession(membership.office_id, { caseId, sessionDate: date, caseStatus: description || "محدد" }); if (selected === "expense") item = await queueExpense(membership.office_id, { scope, caseId: scope === "case" ? caseId : undefined, officeFileId: scope === "file" ? fileId.trim() : undefined, amount: Number(amount), expenseDate: date, category, description }); if (selected === "income") item = await queueIncome(membership.office_id, { scope, caseId: scope === "case" ? caseId : undefined, officeFileId: scope === "file" ? fileId.trim() : undefined, amount: Number(amount), transactionDate: date, category, description }); if (selected === "fee") item = await queueFee(membership.office_id, { caseId, total: Number(amount), notes: description }); if (selected === "payment") item = await queuePayment(membership.office_id, { caseId, amount: Number(amount), date, note: description }); setStatus(offline.isOnline ? "تم الحفظ وإرسال العملية للمزامنة." : "تم الحفظ محليًا؛ ستتم المزامنة عند عودة الاتصال."); setReceipt(`إيصال الحركة · ${date} · ${selected === "session" ? "جلسة" : selected === "expense" ? "مصروف" : "متحصل"}${amount ? ` · ${amount}` : ""}${item?.operationId ? ` · رقم ${item.operationId.slice(0, 8)}` : ""}`); setAmount(""); setDescription(""); } catch (e) { setError(e instanceof Error ? e.message : "تعذر حفظ العملية."); } finally { setSaving(false); } };
  return <ScreenContainer className="px-5" safeAreaClassName="bg-background"><KeyboardAvoidingView behavior={Platform.OS === "ios" ? "padding" : undefined} style={{ flex: 1 }}><ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingTop: 18, paddingBottom: 36 }}><View className="mb-5" style={{ direction: "rtl" }}><Text className="text-2xl font-bold text-foreground">إضافة سريعة</Text><Text className="text-sm text-muted mt-1">قبض ومصروفات وجلسات حتى دون اتصال.</Text></View><View className="flex-row flex-wrap justify-between mb-5" style={{ direction: "rtl" }}>{actions.map(a => <Pressable key={a.key} onPress={() => { setSelected(a.key); setError(""); }} style={{ width: "48.2%", backgroundColor: selected === a.key ? colors.foreground : colors.surface, borderWidth: 1, borderColor: selected === a.key ? colors.foreground : colors.border, borderRadius: 16, padding: 16, marginBottom: 12 }}><IconSymbol name={a.icon} size={23} color={colors.primary} /><Text className="text-sm font-bold mt-3" style={{ color: selected === a.key ? colors.background : colors.foreground }}>{a.label}</Text><Text className="text-xs mt-1" style={{ color: selected === a.key ? "#B9C5D0" : colors.muted }}>{a.hint}</Text></Pressable>)}</View><View className="rounded-3xl p-5" style={{ backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, direction: "rtl" }}><Text className="text-xl font-bold text-foreground mb-5">{selectedAction.label}</Text>{!membership ? <ActivityIndicator color={colors.primary} /> : <><View className="flex-row mb-4" style={{ direction: "rtl" }}>{(["office", "case", "file"] as const).map(s => <Pressable key={s} onPress={() => setScope(s)} style={{ borderRadius: 12, paddingHorizontal: 12, paddingVertical: 8, marginLeft: 8, backgroundColor: scope === s ? colors.primary : colors.background }}><Text style={{ color: scope === s ? colors.background : colors.foreground, fontWeight: "700", fontSize: 12 }}>{s === "office" ? "المكتب" : s === "case" ? "قضية" : "ملف"}</Text></Pressable>)}</View>{(selected === "session" || selected === "fee" || selected === "payment" || scope === "case") && <><Field label="بحث القضية" value={caseSearch} onChangeText={setCaseSearch} placeholder="اسم العميل أو الخصم أو الكود" />{caseOptions.slice(0, 8).map(item => <Pressable key={item.id} onPress={() => { setCaseId(item.id); setCaseSearch(`${item.case_code ?? item.id} · ${item.client_name ?? "بدون اسم"}`); }} style={{ padding: 10, borderWidth: 1, borderColor: item.id === caseId ? colors.primary : colors.border, borderRadius: 10, marginBottom: 6, backgroundColor: colors.background }}><Text className="text-sm font-bold text-foreground" style={{ textAlign: "right" }}>{item.case_code || item.id}</Text><Text className="text-xs text-muted mt-1" style={{ textAlign: "right" }}>{item.client_name || "بدون اسم"}{item.opponent_name ? ` · الخصم: ${item.opponent_name}` : ""}</Text></Pressable>)}</>}{(selected === "expense" || selected === "income") && scope === "file" && <Field label="رمز الملف" value={fileId} onChangeText={setFileId} placeholder="مثال: RE-01-2026-ABC123" />}<Field label={selected === "session" ? "تاريخ الجلسة" : "التاريخ"} value={date} onChangeText={setDate} placeholder="YYYY-MM-DD" />{(selected === "expense" || selected === "income") && <Field label={selected === "income" ? "نوع المتحصل" : "نوع المصروف"} value={category} onChangeText={setCategory} placeholder="تشغيلية / أتعاب" />}{selected !== "session" && <Field label={selected === "fee" ? "إجمالي الأتعاب" : "المبلغ"} value={amount} onChangeText={setAmount} placeholder="0.00" keyboardType="numeric" />}<Field label="ملاحظة اختيارية" value={description} onChangeText={setDescription} placeholder="اكتب ملاحظة مختصرة" />{error ? <Text className="text-sm mb-4" style={{ color: colors.error }}>{error}</Text> : null}{status ? <Text className="text-sm mb-2" style={{ color: colors.success }}>{status}</Text> : null}{receipt ? <View className="rounded-xl p-3 mb-4" style={{ backgroundColor: `${colors.success}18`, borderWidth: 1, borderColor: colors.success }}><Text className="text-xs text-foreground" style={{ textAlign: "right" }}>{receipt}</Text></View> : null}<Pressable onPress={submit} disabled={saving} style={{ backgroundColor: colors.primary, borderRadius: 16, paddingVertical: 15, alignItems: "center", opacity: saving ? .6 : 1 }}>{saving ? <ActivityIndicator color={colors.background} /> : <Text className="text-base font-bold" style={{ color: colors.background }}>حفظ العملية</Text>}</Pressable></>}</View></ScrollView></KeyboardAvoidingView></ScreenContainer>;
}
