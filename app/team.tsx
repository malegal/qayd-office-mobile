import { useEffect, useState } from "react";
import { ActivityIndicator, Pressable, ScrollView, Text, TextInput, View } from "react-native";
import { router } from "expo-router";
import { useColors } from "@/hooks/use-colors";
import { getCurrentMembership } from "@/lib/office-data";
import { createInvite, createRecoveryCodes, listOfficeMembers, revokeMember, setMemberRole, type InviteRole } from "@/lib/office-onboarding";

type Member = { user_id: string; display_name: string | null; role: string; created_at: string };
export default function TeamScreen() { const colors = useColors(); const [officeId,setOfficeId]=useState(""); const [members,setMembers]=useState<Member[]>([]); const [contact,setContact]=useState(""); const [role,setRole]=useState<InviteRole>("lawyer"); const [code,setCode]=useState(""); const [recovery,setRecovery]=useState<string[]>([]); const [error,setError]=useState(""); const [busy,setBusy]=useState(false); const load=async()=>{const m=await getCurrentMembership(); if(!m||m.role!=="manager"){router.replace("/(tabs)");return;} setOfficeId(m.office_id); setMembers(await listOfficeMembers(m.office_id));}; useEffect(()=>{load().catch(()=>setError("تعذر تحميل أعضاء المكتب."));},[]); const invite=async()=>{if(!contact.trim())return;setBusy(true);setError("");try{const x=await createInvite(officeId,contact,role);setCode(x.code);setContact("");}catch(e){setError(e instanceof Error?e.message:"تعذر إنشاء الدعوة.");}finally{setBusy(false);}}; const recover=async()=>{try{setRecovery(await createRecoveryCodes(officeId));}catch(e){setError(e instanceof Error?e.message:"تعذر إنشاء رموز الاسترداد.");}}; if(!officeId&&!error)return <View style={{flex:1,alignItems:"center",justifyContent:"center",backgroundColor:colors.background}}><ActivityIndicator color={colors.primary}/></View>; return <ScrollView contentContainerStyle={{padding:20,backgroundColor:colors.background}}><View style={{direction:"rtl"}}><Pressable onPress={()=>router.back()}><Text style={{color:colors.primary,fontWeight:"700"}}>رجوع</Text></Pressable><Text className="text-2xl font-bold text-foreground mt-5">إدارة فريق المكتب</Text><Text className="text-sm text-muted mt-2 mb-5">الدعوات مؤقتة وتستخدم مرة واحدة، ولا تمنح دور المالك.</Text><View className="rounded-2xl p-4 mb-5" style={{backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border}}><Text className="text-base font-bold text-foreground mb-3">دعوة عضو</Text><TextInput value={contact} onChangeText={setContact} placeholder="البريد أو الهاتف" placeholderTextColor={colors.muted} className="border border-border rounded-xl px-3 py-3 text-foreground mb-3" style={{backgroundColor:colors.background,textAlign:"left"}}/><View className="flex-row mb-3">{([['lawyer','محامي'],['staff','سكرتيرة'],['accountant','محاسب']] as const).map(([r,label])=><Pressable key={r} onPress={()=>setRole(r)} style={{flex:1,padding:10,marginLeft:6,borderRadius:10,backgroundColor:role===r?colors.primary:colors.background}}><Text style={{textAlign:"center",color:role===r?colors.background:colors.foreground,fontWeight:"700"}}>{label}</Text></Pressable>)}</View><Pressable onPress={invite} disabled={busy} style={{backgroundColor:colors.primary,borderRadius:12,padding:13,alignItems:"center"}}><Text style={{color:colors.background,fontWeight:"700"}}>إنشاء كود الدعوة</Text></Pressable>{code?<Text className="text-xl font-bold mt-4" style={{color:colors.primary,textAlign:"center"}}>{code}</Text>:null}</View><View className="rounded-2xl p-4 mb-5" style={{backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border}}><Text className="text-base font-bold text-foreground mb-2">أعضاء المكتب</Text>{members.map(m=><View key={m.user_id} className="py-3 border-b border-border"><Text className="text-sm font-bold text-foreground">{m.display_name||m.user_id}</Text><Text className="text-xs text-muted mt-1">{m.role}</Text>{m.role!=="manager"?<Pressable onPress={()=>revokeMember(officeId,m.user_id).then(load)}><Text className="text-xs mt-2" style={{color:colors.error}}>إزالة من المكتب</Text></Pressable>:null}</View>)}</View><Pressable onPress={recover} className="rounded-xl p-4" style={{backgroundColor:colors.foreground}}><Text style={{color:colors.primary,fontWeight:"700",textAlign:"center"}}>إنشاء رموز استرداد جديدة للمالك</Text></Pressable>{recovery.length?<Text className="text-sm text-foreground mt-3" style={{textAlign:"center"}}>{recovery.join("\n")}</Text>:null}{error?<Text className="text-sm mt-4" style={{color:colors.error}}>{error}</Text>:null}</View></ScrollView>; }
